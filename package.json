{
  "name": "purrbeat",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "prepare:mediapipe": "node scripts/prepare-mediapipe-assets.mjs",
    "prebuild": "npm run prepare:mediapipe",
    "build": "node scripts/build.mjs",
    "deploy": "node scripts/deploy.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.32"
  }
}
